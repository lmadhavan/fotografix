<Page
    x:Class="Fotografix.Editor.ImageEditorPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:canvas="using:Microsoft.Graphics.Canvas.UI.Xaml"
    xmlns:a="using:Fotografix.Editor.Adjustments"
    xmlns:aui="using:Fotografix.Editor.Adjustments.UI"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    Unloaded="Page_Unloaded"
    AllowDrop="True">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="300" />
        </Grid.ColumnDefinitions>
        
        <canvas:CanvasControl x:Name="canvas" Grid.Column="0" CreateResources="Canvas_CreateResources" Draw="Canvas_Draw" />

        <StackPanel Grid.Column="1" Orientation="Vertical" Background="{ThemeResource AppBarBackgroundThemeBrush}" Padding="8">
            <TextBlock Style="{StaticResource SubtitleTextBlockStyle}" Text="Black &amp; White" Margin="0,0,0,8" />
            <StackPanel Orientation="Horizontal">
                <ComboBox Margin="0,0,8,0" MinWidth="125" ItemsSource="{x:Bind viewModel.BlendModes}" SelectedIndex="{x:Bind viewModel.SelectedBlendModeIndex, Mode=TwoWay}" />
                <Button Content="Add" Click="{x:Bind viewModel.AddBlackAndWhiteAdjustment}" />
            </StackPanel>

            <TextBlock Style="{StaticResource SubtitleTextBlockStyle}" Text="Shadows / Highlights" Margin="0,16,0,8" />
            <StackPanel Orientation="Vertical">
                <Button Content="Add" Click="{x:Bind viewModel.AddShadowsHighlightsAdjustment}" />
            </StackPanel>

            <TextBlock Style="{StaticResource SubtitleTextBlockStyle}" Text="Adjustments" Margin="0,16,0,8" />
            <ListView ItemsSource="{x:Bind viewModel.Adjustments}" SelectedItem="{x:Bind viewModel.SelectedAdjustment, Mode=TwoWay}">
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="a:Adjustment">
                        <TextBlock Text="{x:Bind Name, Mode=OneWay}" />
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

            <StackPanel Orientation="Vertical" Margin="0,16,0,0" Padding="6" Background="{ThemeResource NavigationViewExpandedPaneBackground}">
                <StackPanel.Resources>
                    <aui:AdjustmentTemplatesResourceDictionary />
                </StackPanel.Resources>
                
                <TextBox Text="{x:Bind viewModel.SelectedAdjustment.Name, Mode=TwoWay}" />
                <ContentControl Margin="0,8,0,0" HorizontalContentAlignment="Stretch" ContentTemplateSelector="{StaticResource AdjustmentTemplateSelector}" Content="{x:Bind viewModel.SelectedAdjustment, Mode=OneWay}" />
            </StackPanel>
        </StackPanel>
    </Grid>
</Page>
