<Page
    x:Class="Fotografix.Editor.ImageEditorPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:canvas="using:Microsoft.Graphics.Canvas.UI.Xaml"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    Unloaded="Page_Unloaded">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="300" />
        </Grid.ColumnDefinitions>
        
        <canvas:CanvasControl x:Name="canvas" Grid.Column="0" CreateResources="Canvas_CreateResources" Draw="Canvas_Draw" />

        <StackPanel Grid.Column="1" Orientation="Vertical" Background="{ThemeResource AppBarBackgroundThemeBrush}" Padding="8">
            <TextBlock Style="{StaticResource SubtitleTextBlockStyle}" Text="Black &amp; White" Margin="0,0,0,8" />
            <StackPanel Orientation="Horizontal">
                <ComboBox Margin="0,0,8,0" MinWidth="125" ItemsSource="{x:Bind viewModel.BlendModes}" SelectedIndex="{x:Bind viewModel.SelectedBlendModeIndex, Mode=TwoWay}" />
                <Button Content="Apply" Click="{x:Bind viewModel.ApplyBlackAndWhiteAdjustment}" />
            </StackPanel>
        </StackPanel>
    </Grid>
</Page>
