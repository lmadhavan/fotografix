using Windows.System;

namespace Fotografix.Uwp.Input
{
    public sealed class ExtendedVirtualKeys
    {
        public VirtualKey Plus => (VirtualKey)187;
        public VirtualKey Minus => (VirtualKey)189;
    }
}
